# Автоматический выбор менеджеров

## Обзор

Система автоматического выбора менеджеров для консультаций реализует интеллектуальное распределение нагрузки между менеджерами на основе:
- Навыков менеджеров (разделы программы)
- Текущей загрузки (очереди)
- Лимитов менеджеров
- Времени работы

## Компоненты

### 1. ManagerSelector (`FastAPI/services/manager_selector.py`)

Основной сервис для выбора менеджеров.

**Основные методы:**
- `get_available_managers()` - получить список доступных менеджеров
- `select_manager_for_consultation()` - выбрать менеджера для консультации
- `get_manager_queue_count()` - получить количество консультаций в очереди
- `get_manager_current_load()` - получить текущую загрузку менеджера
- `calculate_wait_time()` - рассчитать примерное время ожидания
- `get_all_managers_load()` - получить загрузку всех менеджеров

**Логика выбора:**
1. Фильтрация по лимитам (`con_limit > 0`)
2. Фильтрация по времени работы (`start_hour`, `end_hour`)
3. Фильтрация по навыкам (`users_skill.category_key`)
4. Выбор менеджера с наименьшей загрузкой (очередью)

### 2. ManagerNotifications (`FastAPI/services/manager_notifications.py`)

Сервис для отправки уведомлений.

**Основные функции:**
- `send_manager_reassignment_notification()` - уведомление о переназначении менеджера
- `send_queue_update_notification()` - уведомление об изменении очереди

### 3. API Endpoints (`FastAPI/routers/managers.py`)

**Endpoints:**
- `GET /api/managers/load` - загрузка всех менеджеров
- `GET /api/managers/{manager_key}/load` - загрузка конкретного менеджера
- `GET /api/managers/{manager_key}/wait-time` - время ожидания для менеджера
- `GET /api/managers/available` - список доступных менеджеров
- `GET /api/managers/consultations/{cons_id}/queue-info` - информация об очереди для консультации

## Интеграция

### При создании консультации

1. Автоматически выбирается менеджер на основе:
   - Категории вопроса (`online_question_cat`)
   - Текущей загрузки менеджеров
   - Лимитов и времени работы

2. Менеджер назначается в:
   - БД (`cons.cons.manager`)
   - Chatwoot (`assignee_id`)
   - ЦЛ (`Менеджер_Key`)

3. Клиенту отправляется сообщение с информацией:
   - О принятии заявки
   - О позиции в очереди
   - О примерном времени ожидания

### При переназначении менеджера

**Из ЦЛ (через ETL):**
- `pull_cons_cl.py` обнаруживает изменение `Менеджер_Key`
- Обновляет `cons.cons.manager` в БД
- Отправляет уведомление в Chatwoot

**Из Chatwoot (через webhook):**
- `webhooks.py` обрабатывает событие `conversation.updated`
- Обновляет `cons.cons.manager` в БД
- Синхронизирует с ЦЛ через OData
- Отправляет уведомление клиенту

**Из фронта (через API):**
- Можно добавить endpoint для ручного переназначения
- Логика аналогична webhook от Chatwoot

## Синхронизация с Chatwoot

### Маппинг менеджеров

Используется таблица `sys.user_mapping`:
- `chatwoot_user_id` - ID пользователя в Chatwoot
- `cl_manager_key` - ключ менеджера в ЦЛ (GUID)

При назначении менеджера:
1. Ищем `chatwoot_user_id` по `cl_manager_key` в `user_mapping`
2. Если не найдено, ищем в `cons.users` по `cl_ref_key`
3. Используем `chatwoot_user_id` для назначения в Chatwoot

### Создание менеджеров в Chatwoot

При создании менеджера в Chatwoot:
1. Сохраняем `chatwoot_user_id` в `cons.users.chatwoot_user_id`
2. Создаем запись в `sys.user_mapping`:
   - `chatwoot_user_id` = ID из Chatwoot
   - `cl_manager_key` = `cl_ref_key` из `cons.users`

## Расчет очереди

Очередь считается как количество консультаций со статусом:
- `pending` или `open`
- `denied = False`
- `manager` = ключ менеджера

**Примерное время ожидания:**
- `estimated_wait_minutes = queue_count * average_duration_minutes`
- `estimated_wait_hours = round(estimated_wait_minutes / 60)`

По умолчанию `average_duration_minutes = 60`.

## Уведомления

### О переназначении менеджера

Отправляется сообщение в Chatwoot:
```
👤 Менеджер переназначен
Было: [старый менеджер]
Стало: [новый менеджер]
Причина: [причина, если указана]
```

### Об изменении очереди

Отправляется сообщение в Chatwoot:
```
📊 Вы в очереди #[позиция]. Примерное время ожидания: [часы] [часов/часа/час].
```

## Настройка

### Лимиты менеджеров

Устанавливаются в таблице `cons.users`:
- `con_limit` - максимальное количество консультаций в очереди

### Время работы

Устанавливается в таблице `cons.users`:
- `start_hour` - время начала работы (TIME)
- `end_hour` - время окончания работы (TIME)

Если не установлено, менеджер считается работающим всегда.

### Навыки менеджеров

Хранятся в таблице `cons.users_skill`:
- `user_key` - ключ менеджера (cl_ref_key)
- `category_key` - ключ категории вопроса (КатегорияВопроса_Key)

Если у менеджера нет навыков, он считается универсальным (знает все разделы).

## Примеры использования

### Получить загрузку всех менеджеров

```bash
GET /api/managers/load
```

Ответ:
```json
[
  {
    "manager_key": "guid-менеджера",
    "manager_id": "uuid-account-id",
    "chatwoot_user_id": 123,
    "name": "Иван Иванов",
    "queue_count": 5,
    "limit": 10,
    "load_percent": 50.0,
    "available_slots": 5,
    "start_hour": "09:00:00",
    "end_hour": "18:00:00"
  }
]
```

### Получить время ожидания для менеджера

```bash
GET /api/managers/{manager_key}/wait-time?average_duration_minutes=60
```

Ответ:
```json
{
  "queue_position": 6,
  "estimated_wait_minutes": 300,
  "estimated_wait_hours": 5
}
```

### Получить информацию об очереди для консультации

```bash
GET /api/managers/consultations/{cons_id}/queue-info
```

Ответ:
```json
{
  "queue_position": 3,
  "estimated_wait_minutes": 120,
  "estimated_wait_hours": 2,
  "manager_key": "guid-менеджера"
}
```

## TODO / Улучшения

1. **Маппинг разделов программы:**
   - Добавить прямую связь между `po_section_key` и `category_key`
   - Улучшить выбор менеджеров по разделу программы

2. **Умное распределение:**
   - Учитывать историю работы менеджера с клиентом
   - Учитывать приоритет консультации
   - Учитывать язык консультации

3. **Автоматическое обновление очереди:**
   - Периодически пересчитывать очередь
   - Отправлять уведомления при изменении позиции

4. **Метрики и аналитика:**
   - Среднее время обработки консультации
   - Загрузка менеджеров по времени
   - Эффективность распределения

